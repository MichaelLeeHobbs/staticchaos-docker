# world-maps/ is generated by `pnpm run build:data` (JSON/MD/HTML) and
# `pnpm run build:pdf*` (PDFs). Build outputs -- not committed (#65). This
# .gitkeep keeps the dir present so the web Docker build COPY never fails.
